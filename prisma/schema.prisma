generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model Carro {
  id     String @id @default(uuid())
  marca  String
  modelo String
  ano    Int
  preco  Float
}
